{
  "name": "pantrymind",
  "version": "0.0.0-pre",
  "description": "AI-native, self-hostable food and health memory for your assistant.",
  "license": "MIT",
  "author": "Ludovic Micinthe",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "prettier --check \"**/*.{ts,tsx,js,mjs,json,md,yml,yaml}\" --ignore-path .gitignore",
    "format": "prettier --write \"**/*.{ts,tsx,js,mjs,json,md,yml,yaml}\" --ignore-path .gitignore",
    "test": "turbo run test",
    "test:unit": "turbo run test:unit",
    "test:api": "pnpm --filter @pantrymind/api test",
    "test:mcp": "pnpm --filter @pantrymind/mcp test",
    "test:e2e": "pnpm --filter @pantrymind/api test:e2e",
    "db:migrate": "pnpm --filter @pantrymind/db migrate",
    "db:seed": "pnpm --filter @pantrymind/db seed",
    "demo:dry-run": "pnpm --filter @pantrymind/api demo:dry-run",
    "verify": "pnpm typecheck && pnpm lint && pnpm test && pnpm build",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "prettier": "^3.4.0",
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  }
}
